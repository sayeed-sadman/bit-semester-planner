{
  "name": "BIT Semester Planner",
  "image": "mcr.microsoft.com/devcontainers/java:21",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "20"
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y tmux && cd /workspaces/bit-semester-planner && chmod +x mvnw && chmod +x start.sh && ./mvnw dependency:resolve -q && cd frontend && npm install",
  "postStartCommand": "bash /workspaces/bit-semester-planner/start.sh && tmux attach -t bitsemesterplanner",
  "forwardPorts": [8080, 5173],
  "portsAttributes": {
    "8080": { "label": "Backend API", "onAutoForward": "notify" },
    "5173": { "label": "Frontend", "onAutoForward": "openBrowser" }
  }
}
